--- a/drivers/power/pmic/pmic.c
+++ b/drivers/power/pmic/pmic.c
@@ -120,7 +120,14 @@ static void battery_saving_mode(void)
-    pm_set_deep_sleep(30); 
+    pm_set_deep_sleep(50); /* Enhanced idle efficiency */
+
+    /* BGMI-specific power tweaks */
+    if (app_is_running("com.pubg.imobile")) {
+        limit_cpu_hotplug(4); /* Prevent unnecessary CPU wake-ups */
+        activate_cooler_mode(); /* Keep device cool */
+        dynamic_voltage_scaling(LOW); /* Lower voltage during steady FPS */
+    }
